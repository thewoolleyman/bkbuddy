<!DOCTYPE html>
<html>
  <head>
    <title>bkbuddy - Buildkite Buddy</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div>
      <% if !session[:oktastate] %>
        <span><%= link_to "Sign In", user_oktaoauth_omniauth_authorize_path %></span>
      <% else %>
        <span><%= session[:oktastate][:info][:name] %></span>
        <span><%= link_to "logout", ENV['OKTA_URL'] + "/login/signout?fromURI=" + request.base_url + "/logout" %></span>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
